<!DOCTYPE html>
<html>
  <head>
  	<% if (tutorid != "") { %>
	  	<title>Events for Tutor: <%= tutorid %></title>
  	<% } else { %>
	    <title>All Events</title>
	<% } %>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
  	<div id="header"><em>Class</em>RATE</div>
  	
  	<% if (tutorid) {%>
		<h1>Events for tutor <%=tutorid%></h1>
	<% } else { %>
	    <h1>All events</h1>
    <% } %>
    
    <ul>
	<% for(var i=0; i<events.length; i++) {%>
	   <li><a href="/event/<%=events[i]._id%>"><%= events[i].name %></a></li>
	<% } %>
	</ul>
    
  </body>
</html>
